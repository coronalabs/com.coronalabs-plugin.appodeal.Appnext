Sources for the plugin `plugin.appodeal.Appnext`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Appnext" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
